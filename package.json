{
  "name": "@density/proto-ui-colors",
  "version": "1.0.2",
  "description": "",
  "files": [
    "dist/**/*"
  ],
  "main": "dist/index.js",
  "style": "dist/index.scss",
  "scripts": {
    "build": "npm run build:js && npm run build:scss && npm run build:source",
    "build:js": "tsc -p .",
    "build:scss": "node build-stylesheet",
    "build:source": "cp ./src/index.ts ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Density",
  "repository": {
    "type": "git",
    "url": "https://github.com/DensityCo/proto-ui-colors.git"
  },
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
